import { moblibNativescript } from './moblib-nativescript';

describe('moblibNativescript', () => {
  it('should work', () => {
    expect(moblibNativescript()).toEqual('moblib-nativescript');
  });
});
